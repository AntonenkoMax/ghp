import {
  LayoutProps,
  SpaceProps,
  TypographyProps,
  OpacityProps,
  FlexboxProps,
} from "styled-system";

export interface TextProps
  extends SpaceProps,
    TypographyProps,
    LayoutProps,
    OpacityProps,
    FlexboxProps {}
